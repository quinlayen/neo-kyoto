# ── Underground Plant — Water Treatment ─────────
#
# Write your program below.
#
# Your available commands:
#   scan_system()             — show all unit statuses
#   get_broken_stations()     — list of broken pump
#                                station IDs
#   get_broken_valves()       — list of broken intake
#                                valve IDs
#   drain(unit_id)            — step 1: drain
#   flush(unit_id)            — step 2: flush
#   refill(unit_id)           — step 3: refill
#   restart(unit_id)          — step 4: restart
#
# ────────────────────────────────────────────────

