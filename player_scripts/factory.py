# ── Industrial Zone — Sector Sweep ──────────────
#
# Write your program below.
#
# Your available commands:
#   scan_floor()              — show all line statuses
#   get_broken_lines()        — returns a list of broken
#                                line IDs
#   diagnose(line_id)         — returns the failure mode
#   clear_jam(line_id)        — fix a JAMMED line
#   cool_down(line_id)        — fix an OVERHEATED line
#   restart_line(line_id)     — restart after fixing
#
# You can define your own functions with def:
#
#   def my_function(param):
#       <do something with param>
#
# ────────────────────────────────────────────────

