# ── Transit Hub — Signal Control ───────────────
#
# Write your program below.
#
# Your available commands:
#   check_signal(n)      — shows and returns the
#                           signal's state
#   reset_signal(n)      — fixes STUCK signals
#   calibrate_signal(n)  — fixes SCRAMBLED signals
#   submit_report()      — log your completed work
#
# Signal numbers: 1 through 6
#
# ────────────────────────────────────────────────

